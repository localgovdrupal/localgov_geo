localgov_geo.autocomplete:
  path: /localgov_geo/{settings_key}
  defaults:
    _controller: \Drupal\localgov_geo\Controller\AutocompleteController::autocomplete
  # As core autocomplete this checks the settings key in the controller.
  requirements:
    _access: 'TRUE'
